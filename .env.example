# Zernio powers real social posting — see README.md "Real posting setup"
# for the full walkthrough. Both are required for /dashboard/connections
# to do anything real; without them, the app runs fine in "not configured"
# mode (no fake posting, honest empty state).

ZERNIO_API_KEY=
ZERNIO_PROFILE_ID=
